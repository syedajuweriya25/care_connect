// This file is used to create the login view for the mobile app
import 'package:flutter/material.dart'; 
class LoginView extends StatelessWidget {
  // This is the constructor for the LoginView
  const LoginView({super.key});

  // This is the build method for the LoginView
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Mobile Login View")));
  }
}